import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function VisitDetailPage() {
  return (
    <div className="p-6">
      <Card className="dockflow-card">
        <CardHeader>
          <CardTitle>VisitDetailPage</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the VisitDetailPage component. Implementation coming soon!</p>
        </CardContent>
      </Card>
    </div>
  );
}
