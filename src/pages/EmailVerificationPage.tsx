import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function EmailVerificationPage() {
  return (
    <div className="p-6">
      <Card className="dockflow-card">
        <CardHeader>
          <CardTitle>EmailVerificationPage</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the EmailVerificationPage component. Implementation coming soon!</p>
        </CardContent>
      </Card>
    </div>
  );
}
