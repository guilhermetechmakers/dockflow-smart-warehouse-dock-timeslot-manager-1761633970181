import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function SchedulePage() {
  return (
    <div className="p-6">
      <Card className="dockflow-card">
        <CardHeader>
          <CardTitle>SchedulePage</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the SchedulePage component. Implementation coming soon!</p>
        </CardContent>
      </Card>
    </div>
  );
}
