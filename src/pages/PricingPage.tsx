import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function PricingPage() {
  return (
    <div className="p-6">
      <Card className="dockflow-card">
        <CardHeader>
          <CardTitle>PricingPage</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the PricingPage component. Implementation coming soon!</p>
        </CardContent>
      </Card>
    </div>
  );
}
