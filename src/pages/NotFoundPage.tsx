import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function NotFoundPage() {
  return (
    <div className="p-6">
      <Card className="dockflow-card">
        <CardHeader>
          <CardTitle>NotFoundPage</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the NotFoundPage component. Implementation coming soon!</p>
        </CardContent>
      </Card>
    </div>
  );
}
