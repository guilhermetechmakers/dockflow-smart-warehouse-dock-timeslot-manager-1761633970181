import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function GateCheckInPage() {
  return (
    <div className="p-6">
      <Card className="dockflow-card">
        <CardHeader>
          <CardTitle>GateCheckInPage</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the GateCheckInPage component. Implementation coming soon!</p>
        </CardContent>
      </Card>
    </div>
  );
}
