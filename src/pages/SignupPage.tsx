import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function SignupPage() {
  return (
    <div className="p-6">
      <Card className="dockflow-card">
        <CardHeader>
          <CardTitle>SignupPage</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the SignupPage component. Implementation coming soon!</p>
        </CardContent>
      </Card>
    </div>
  );
}
