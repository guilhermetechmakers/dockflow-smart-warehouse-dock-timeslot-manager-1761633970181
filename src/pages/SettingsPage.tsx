import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function SettingsPage() {
  return (
    <div className="p-6">
      <Card className="dockflow-card">
        <CardHeader>
          <CardTitle>SettingsPage</CardTitle>
        </CardHeader>
        <CardContent>
          <p>This is the SettingsPage component. Implementation coming soon!</p>
        </CardContent>
      </Card>
    </div>
  );
}
